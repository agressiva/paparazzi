<!DOCTYPE module SYSTEM "./module.dtd">

<module name="consume per minute"  dir="benchmark">
  <doc>
    <description>
      Faz a media de consumo por minuto
    </description>
  </doc>
  <header>
    <file name="consume_rotorcraft.h"/>
  </header>

  <periodic fun="consume_periodic()" freq="1" autorun="TRUE"/>

  <makefile>
    <file name="consume_rotorcraft.c"/>
  </makefile>

</module>